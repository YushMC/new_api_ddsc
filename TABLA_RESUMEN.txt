================================================================================
TABLA RESUMEN: RUTAS, SERVICIOS Y NOTIFICACIONES - MODS
================================================================================

RUTA                                    | HTTP   | SERVICIO              | ESTADO
────────────────────────────────────────────────────────────────────────────────
/api/mods                               | POST   | create_mod            | ✅
/api/mods/{id}                          | PUT    | update_mod            | ✅
/api/mods/{id}                          | DELETE | delete_mod            | ⚠️
/api/mods/{id}/approve                  | POST   | approve_mod           | ✅
/api/mods/{id}/rejected                 | POST   | reject_mod            | ✅
/api/mods/{id}/restore                  | POST   | restore_mod           | ❌
/api/mods/{id}/genres                   | POST   | add_genres_to_mod     | ✅
/api/mods/{id}/genres                   | DELETE | remove_genres_from_mod| ✅
/api/mods/{id}/genres                   | GET    | get_mod_genres        | ✅

================================================================================
CAPTURA DE CAMBIOS: MÉTODOS QUE RETORNAN CAMBIOS
================================================================================

MÉTODO            | RETORNA TIPO      | CAMPOS CAPTURADOS
──────────────────────────────────────────────────────────────────────────────
create_mod        | Mod               | Automático: created_by, created_at
update_mod        | (Mod, Dict)       | Todos excepto: created_by, updated_by, created_at, is_active
approve_mod       | (Mod, Dict)       | Manual: required_revision (True→False)
reject_mod        | (Mod, Dict)       | Manual: required_revision + rejected
delete_mod        | Mod               | Automático: is_active (True→False), deleted_by
restore_mod       | Mod               | ❌ NO EXISTE

================================================================================
NOTIFICACIONES DISCORD: MÉTODOS ASYNC
================================================================================

MÉTODO                      | PARÁMETROS              | ESTADO | LÍNEA RUTA
──────────────────────────────────────────────────────────────────────────────
notify_mod_created          | (mod, user)             | ✅     | 160
notify_mod_updated          | (mod, user, changes)    | ✅     | 180
notify_mod_approved         | (mod, approved_by)      | ✅     | 264
notify_mod_rejected         | (mod, rejected_by)      | ✅     | 312
notify_mod_deleted          | (mod, deleted_by)       | ✅     | 221
notify_mod_restored         | (mod, restored_by)      | ✅     | 357
notify_mod_completed        | (mod)                   | ✅     | N/A

COLORES UTILIZADOS:
  notify_mod_created: Rojo (0xFF0000) o Verde (0x00FF00) según rol
  notify_mod_updated: Naranja (0xFFA500)
  notify_mod_approved: Verde oscuro (0x00DD00)
  notify_mod_rejected: Rojo (0xFF0000)
  notify_mod_deleted: Gris (0x808080)
  notify_mod_restored: Verde (0x00DD00)

================================================================================
NOTIFICACIONES BASE DE DATOS: MÉTODOS EN CRUD_NOTIFICATION
================================================================================

MÉTODO                      | PARÁMETROS                           | ESTADO
──────────────────────────────────────────────────────────────────────────────
notify_mod_pending_review   | (mod_id, mod_name, uploader_name)   | ✅
notify_mod_approved         | (mod_id, mod_name, mod_creator_id)  | ✅
notify_mod_rejected         | (mod_id, mod_name, mod_creator_id)  | ✅
notify_mod_deleted          | (mod_id, mod_name, mod_creator_id)  | ❌ FALTA
notify_mod_restored         | (mod_id, mod_name, mod_creator_id)  | ❌ FALTA

MAPEO CON RUTAS:
  POST /api/mods (UPLOADER)      → notify_mod_pending_review() ✅
  POST /api/mods/{id}/approve    → notify_mod_approved() ✅
  POST /api/mods/{id}/rejected   → notify_mod_rejected() ✅
  DELETE /api/mods/{id}          → notify_mod_deleted() ❌ LLAMA MÉTODO QUE NO EXISTE
  POST /api/mods/{id}/restore    → notify_mod_restored() ❌ LLAMA MÉTODO QUE NO EXISTE

================================================================================
MÉTODOS CRUD_MOD DISPONIBLES (12 PÚBLICOS)
================================================================================

CREAR:
  ✅ create_mod(data: ModBase, user: TokenUser) → Mod

LEER:
  ✅ get_mod(mod_id: int) → Mod | None
  ✅ get_mods(skip: int = 0, limit: int = 20) → List[Mod]
  ✅ get_mods_admin(skip: int = 0, limit: int = 20) → List[Mod]
  ✅ get_mod_genres(mod_id: int) → List[Genre]

ACTUALIZAR:
  ✅ update_mod(mod_id: int, data: ModBase, user: TokenUser) → (Mod, Dict)
  ✅ approve_mod(mod_id: int, user: TokenUser) → (Mod, Dict)
  ✅ reject_mod(mod_id: int, user: TokenUser, comments: str) → (Mod, Dict)
  ✅ add_genres_to_mod(mod_id: int, genre_ids: List[int]) → Mod
  ✅ remove_genres_from_mod(mod_id: int, genre_ids: List[int]) → Mod

ELIMINAR:
  ✅ delete_mod(mod_id: int, user: TokenUser) → Mod

UTILIDADES:
  ✅ is_mod_complete(mod_id: int) → bool

================================================================================
AUTENTICACIÓN POR RUTA
================================================================================

RUTA                              | REQUIERE AUTH | SOLO ROL      | DESCRIPCIÓN
──────────────────────────────────────────────────────────────────────────────
POST /api/mods                    | Sí            | -             | Cualquiera
PUT /api/mods/{id}                | Sí            | EDITOR/OWNER  | No UPLOADER
DELETE /api/mods/{id}             | Sí            | EDITOR/OWNER  | No UPLOADER
POST /api/mods/{id}/approve       | Sí            | EDITOR/OWNER  | Solo admin
POST /api/mods/{id}/rejected      | Sí            | EDITOR/OWNER  | Solo admin
POST /api/mods/{id}/restore       | Sí            | EDITOR/OWNER  | Solo admin
POST /api/mods/{id}/genres        | Sí            | -             | Cualquiera
DELETE /api/mods/{id}/genres      | Sí            | -             | Cualquiera
GET /api/mods/{id}/genres         | No            | -             | Público

================================================================================
PROBLEMAS CRÍTICOS: MÉTODOS QUE NO EXISTEN
================================================================================

UBICACIÓN                  | LÍNEA | MÉTODO LLAMADO              | IMPACTO
──────────────────────────────────────────────────────────────────────────────
routes/mods.py             | 339   | crud.restore_mod()          | POST /restore falla
routes/mods.py             | 213   | crud_notif.notify_mod_deleted()    | DELETE falla
routes/mods.py             | 349   | crud_notif.notify_mod_restored()   | POST /restore falla

SOLUCIONES REQUERIDAS:
  1. Implementar restore_mod() en CRUD_MOD
  2. Implementar notify_mod_deleted() en CRUD_NOTIFICATION
  3. Implementar notify_mod_restored() en CRUD_NOTIFICATION

================================================================================
INCONSISTENCIAS: PROBLEMAS DE DISEÑO
================================================================================

PROBLEMA                           | UBICACIÓN              | LÍNEA | SEVERIDAD
──────────────────────────────────────────────────────────────────────────────
delete_mod() no toma parámetro     | routes/mods.py:203    | 203   | ⚠️ MEDIA
reason (se guarda en ruta)         | services/mods.py:188  | 188   |

Búsqueda por nombre ineficiente    | routes/mods.py:254    | 254   | ⚠️ BAJA
filter(User.name == mod.created_by)| routes/mods.py:302    | 302   |

Notificaciones BD incompletas      | services/notif.py     | N/A   | ⚠️ MEDIA
Faltan: notify_mod_deleted()       | routes/mods.py        | 213   |
Faltan: notify_mod_restored()      | routes/mods.py        | 349   |

================================================================================
ESTRUCTURA DE CAMBIOS RETORNADA
================================================================================

Formato:
{
    "field_name": {
        "old": old_value,
        "new": new_value
    }
}

Ejemplo:
{
    "name": {
        "old": "Old Mod Name",
        "new": "New Mod Name"
    },
    "status": {
        "old": "active",
        "new": "pending"
    }
}

Campos EXCLUIDOS (no capturados):
  - created_by, updated_by (auditoría)
  - created_at (marcado de tiempo)
  - is_active (cambios de estado)

================================================================================
CAMPOS DE AUDITORÍA GUARDADOS
================================================================================

CAMPO           | TIPO      | CUÁNDO SE GUARDA    | QUIÉN ACTUALIZA
────────────────────────────────────────────────────────────────────────────────
created_by      | str       | En create           | Usuario que crea
created_at      | datetime  | En create           | Auto (BD)
updated_by      | str       | En cualquier update | Usuario que actualiza
updated_at      | datetime  | En cualquier update | Auto (BD)
deleted_by      | str       | En delete (soft)    | Usuario que elimina
deleted_at      | datetime  | ❌ NO SE GUARDA     | -
approved_by     | str       | En approve_mod      | Usuario que aprueba
approved_at     | datetime  | En approve_mod      | Auto (NOW)
rejected_by     | str       | En reject_mod       | Usuario que rechaza
rejected_at     | datetime  | En reject_mod       | Auto (NOW)
comments        | str       | En reject/delete    | Razón rechazo/eliminación

================================================================================
FLUJOS PRINCIPALES
================================================================================

FLUJO 1: POST /api/mods (CREATE)
────────────────────────────────────────────────────────────────────────────────
Cliente
  ↓
create_mod_route()
  ↓
crud.create_mod(data, user)
  ├─ Si UPLOADER: CRUD_NOTIFICATION.notify_mod_pending_review()
  └─ Retorna: Mod
  ↓
background_tasks.add_task(notify_mod_created, mod, user)
  ↓
notify_mod_created() [wrapper síncrono]
  ↓
DiscordNotifier.notify_mod_created() [async]
  ↓
_format_embed_created() + _send_webhook()
  ↓
Discord Webhook POST
  ↓
Canal Discord

FLUJO 2: PUT /api/mods/{id} (UPDATE)
────────────────────────────────────────────────────────────────────────────────
Cliente
  ↓
update_mod_route()
  ↓
crud.update_mod(id, data, user)
  ├─ Compara old vs new para cada campo
  ├─ Retorna: (Mod, Dict[cambios])
  └─ Si cambio en required_revision (True→False): approved_at = NOW
  ↓
background_tasks.add_task(notify_mod_updated, mod, user, changes)
  ↓
notify_mod_updated() [wrapper síncrono]
  ↓
DiscordNotifier.notify_mod_updated() [async]
  ├─ Si cambio required_revision (True→False):
  │  └─ _format_embed_approved()
  └─ Si otros cambios:
     └─ _format_embed_updated() con detalles
  ↓
_send_webhook()
  ↓
Discord Webhook POST
  ↓
Canal Discord

FLUJO 3: DELETE /api/mods/{id} (DELETE)
────────────────────────────────────────────────────────────────────────────────
Cliente
  ↓
delete_mod_route()
  ↓
crud.delete_mod(id, user)
  ├─ is_active = False
  ├─ deleted_by = user.name
  └─ Retorna: Mod
  ↓
mod.comments = request.reason [GUARDADO EN RUTA]
  ↓
creator = query User by id
  ↓
Si creator existe:
  └─ CRUD_NOTIFICATION.notify_mod_deleted(...) ❌ NO EXISTE
  ↓
background_tasks.add_task(DiscordNotifier.notify_mod_deleted, mod, user)
  ↓
notify_mod_deleted() [wrapper - NO EXISTE EN background_tasks.py]
  ↓
DiscordNotifier.notify_mod_deleted() [async]
  ↓
_format_embed_deleted() + _send_webhook()
  ↓
Discord Webhook POST
  ↓
Canal Discord

================================================================================
ESTADÍSTICAS DEL ANÁLISIS
================================================================================

Métrica                               | Cantidad
──────────────────────────────────────────────────────────────────────────────
Rutas POST/PUT/DELETE analizadas      | 9
Métodos CRUD públicos                 | 12
Métodos Discord (async)               | 7
Métodos Notificación BD (implementados)| 3
Métodos Notificación BD (requeridos)  | 5
Problemas críticos encontrados        | 3
Inconsistencias identificadas         | 3
Líneas de código analizadas           | ~1,937
Documentación generada                | ~51 KB
Documentos creados                    | 6

================================================================================
ARCHIVOS ANALIZADOS
================================================================================

Archivo                              | Líneas | Estado
──────────────────────────────────────────────────────────────────────────────
src/routes/mods.py                   | 432    | ✅ Completo
src/services/mods.py                 | 414    | ✅ Completo
src/utils/discord_notifier.py         | 642    | ✅ Completo
src/services/notifications.py         | 325    | ✅ Completo
src/background_tasks.py               | 92     | ✅ Completo
src/conf/discord_config.py            | 32     | ✅ Completo

TOTAL                                 | 1,937  | ✅ 100% Analizado

================================================================================

Fecha de análisis: 2026-03-20
Modelo: claude-haiku-4.5
Completitud: 100%

Documento generado: TABLA_RESUMEN.txt

